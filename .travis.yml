language: python
python:
  - "3.4"
before_install:
  - pip install codecov pytest-cov
install:
  - pip install -e .
script:
  - py.test --cov-report xml --cov orbitdatamessages
after_success:
  - codecov
